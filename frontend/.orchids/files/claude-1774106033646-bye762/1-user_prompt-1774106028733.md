The "Star Chat" feature is not functioning correctly.

Issue:
- When I right-click a chat and click "Star Chat", nothing happens.
- The chat is not moving to the top.
- UI is not updating (no visual feedback).

Fix this with the following behavior:

----------------------------------------
1. STATE MANAGEMENT (CRITICAL)
----------------------------------------

- Each chat object must have a property:
  isStarred: boolean

- When "Star Chat" is clicked:
  → set isStarred = true for that chat
  → update application state immediately

- When "Unstar Chat" is clicked:
  → set isStarred = false

----------------------------------------
2. SORTING LOGIC
----------------------------------------

- Chats must be dynamically sorted like this:

  1. Starred chats first
  2. Then normal chats
  3. Within each group → sort by latest updated time

- Sorting should re-run after every star/unstar action

----------------------------------------
3. UI UPDATE (REACTIVITY)
----------------------------------------

- The UI must re-render immediately after state change

- The starred chat should:
  → instantly move to the top
  → not require refresh

- Add visible feedback:
  → star icon filled when starred
  → optional highlight

----------------------------------------
4. STAR LIMIT RULE
----------------------------------------

- Maximum 5 starred chats allowed

If user tries to star more:
  → show message: "Maximum 5 starred chats allowed"

----------------------------------------
5. PERSISTENCE (IMPORTANT)
----------------------------------------

- Starred state must persist:
  → store in localStorage OR backend

- On reload:
  → starred chats should remain starred

----------------------------------------
6. CONTEXT MENU FIX
----------------------------------------

- Ensure right-click menu correctly identifies the selected chat
- No mismatch between clicked chat and updated chat

----------------------------------------
7. DEBUG CHECKS
----------------------------------------

Fix if any of the following issues exist:
- State not updating
- Sorting not applied
- UI not re-rendering
- Event handler not firing

----------------------------------------
EXPECTED RESULT
----------------------------------------

- Click "Star Chat"
→ Chat instantly moves to top
→ Star icon updates
→ State persists
→ No page refresh needed

----------------------------------------
END