----------------------------------------
1. REMOVE "START NEW CHAT" BUTTON
----------------------------------------

- Remove the "+ Start new chat" button from the left sidebar completely.

- Instead, implement this behavior:
  → When the user clicks on the KIWI logo (top-left corner),
    it should:
    - Create a new chat session
    - Clear current messages
    - Load a fresh empty chat screen

This should behave exactly like "New Chat" in ChatGPT.

----------------------------------------
2. USER SETTINGS DROPDOWN (BOTTOM LEFT)
----------------------------------------

- Currently, clicking the user profile/settings icon does nothing.
- Fix this by implementing a dropdown menu.

On click of the user (Dr. K Srinivasa Rao section):

Show a dropdown with:
  1. Settings
  2. Logout

Behavior:
- Dropdown should appear above or near the profile section
- Clean UI, minimal design
- Close when clicking outside

----------------------------------------
3. FIX CHAT HISTORY GROUPING (CRITICAL)
----------------------------------------

Current issue:
- Every message is being treated as a separate chat

Expected behavior:
- A chat session should contain multiple messages
- Messages should remain grouped until a new chat is created

Correct logic:
- Only create a new chat when:
  → User clicks KIWI logo (new chat trigger)

- Otherwise:
  → All prompts go into the SAME chat session

Sidebar behavior:
- Each chat should appear as ONE item
- Title = first message of that chat

----------------------------------------
4. STAR / PIN CHAT FEATURE (REPLACE EXISTING)
----------------------------------------

- Remove the existing "Starred" section and default items

- Implement new behavior:

Right-click on any chat in sidebar:
  → Show context menu:
      - "Star Chat"
      - "Unstar Chat" (if already starred)

Rules:
- Maximum 5 starred chats
- Starred chats appear at top of sidebar
- Clearly separated from normal chats

UI:
- Use subtle star icon or highlight
- Maintain clean, minimal look

----------------------------------------
5. EXPORT BUTTON (OPTIONAL)
----------------------------------------

- Export button currently does not work
- This can be ignored for now OR
- Show a disabled state / "Coming Soon"